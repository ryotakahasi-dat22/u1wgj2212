using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sukoakesi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameManager.score = 0;
    }

    // Update is called once per frame
    void Update()
    {
        GameManager.score=0;
    }
}
